version: 0.0
os: linux
files:
  - source: /customerRebranding.war
    destination: /usr/share/tomcat/webapps
hooks:
  BeforeInstall:
    - location: setup.sh
      timeout: 300
      runas: root
  ApplicationStart:
    - location: start.sh
      timeout: 300
      runas: root
